const initialState-{
    Suggested:[]
}


export const suggestedReducer=(state=initialState,action) =>{
    switch(action.type){
        case SUGG_GET:
            return {
                
            }
    }
}